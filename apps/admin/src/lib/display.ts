// The prefix of a UUID shown as a short reference; one length everywhere, so the same entity reads
// the same on every surface.
export const SHORT_ID_LENGTH = 8

export const INITIALS_MAX_LETTERS = 2
